# Custom Reports

Frontend feature for `/api/reports/custom/*`.

Current backend support:

- Dataset catalog and schema metadata.
- Preview/run by delegating to existing core/pharmacy report APIs.
- Empty professional states for my/shared/pinned/export persistence until custom report models are added.
