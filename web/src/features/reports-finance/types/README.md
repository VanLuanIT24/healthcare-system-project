Finance report payloads are normalized by `/api/reports/finance/*`.

The frontend intentionally consumes plain JavaScript objects because this app is not TypeScript-enabled. Keep this folder as the contract note for future migration to `reportsFinanceApi.ts`.

