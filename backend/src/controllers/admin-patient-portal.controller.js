const service = require('../services/admin-patient-portal.service');
const { controllerHandler: wrap, requestMeta } = require('../common/controllers');

module.exports = {
  getDashboard: wrap(() => service.getDashboard(), 'Lấy dashboard Patient Portal Admin thành công.'),
  getWorkQueue: wrap(() => service.getWorkQueue(), 'Lấy work queue Patient Portal Admin thành công.'),
  getHealth: wrap(() => service.getHealth(), 'Lấy portal health thành công.'),

  listAccounts: wrap((req) => service.listAccounts(req.query), 'Lấy danh sách tài khoản bệnh nhân thành công.'),
  getAccountsSummary: wrap(() => service.getAccountsSummary(), 'Lấy tổng quan tài khoản bệnh nhân thành công.'),
  getAccount: wrap((req) => service.getAccount(req.params.accountId), 'Lấy chi tiết tài khoản bệnh nhân thành công.'),
  updateAccount: wrap((req) => service.updateAccount(req.params.accountId, req.body || {}, req.auth, requestMeta(req)), 'Cập nhật tài khoản bệnh nhân thành công.'),
  lockAccount: wrap((req) => service.lockAccount(req.params.accountId, req.body || {}, req.auth, requestMeta(req)), 'Khóa tài khoản bệnh nhân thành công.'),
  unlockAccount: wrap((req) => service.unlockAccount(req.params.accountId, req.auth, requestMeta(req)), 'Mở khóa tài khoản bệnh nhân thành công.'),
  disableAccount: wrap((req) => service.disableAccount(req.params.accountId, req.auth, requestMeta(req)), 'Disable tài khoản bệnh nhân thành công.'),
  enableAccount: wrap((req) => service.enableAccount(req.params.accountId, req.auth, requestMeta(req)), 'Enable tài khoản bệnh nhân thành công.'),
  resetAccountPassword: wrap((req) => service.resetAccountPassword(req.params.accountId, req.body || {}, req.auth, requestMeta(req)), 'Đánh dấu reset mật khẩu thành công.'),
  forceLogoutAccount: wrap((req) => service.forceLogoutAccount(req.params.accountId, req.body || {}, req.auth, requestMeta(req)), 'Force logout tài khoản bệnh nhân thành công.'),
  resendVerification: wrap((req) => service.resendVerification(req.params.accountId, req.body || {}, req.auth, requestMeta(req)), 'Ghi nhận yêu cầu gửi lại xác thực thành công.'),
  unlinkGoogle: wrap((req) => service.unlinkGoogle(req.params.accountId, req.body || {}, req.auth, requestMeta(req)), 'Gỡ liên kết Google thành công.'),
  listAccountSessions: wrap((req) => service.listAccountSessions(req.params.accountId, req.query), 'Lấy session tài khoản bệnh nhân thành công.'),
  listAccountAudit: wrap((req) => service.listAccountAudit(req.params.accountId, req.query), 'Lấy audit tài khoản bệnh nhân thành công.'),
  listAccountLoginHistory: wrap((req) => service.listAccountAudit(req.params.accountId, { ...req.query, action: req.query.action || 'auth.login' }), 'Lấy login history tài khoản bệnh nhân thành công.'),

  listRelatives: wrap((req) => service.listRelatives(req.query), 'Lấy danh sách người thân bệnh nhân thành công.'),
  getRelativesSummary: wrap(() => service.getRelativesSummary(), 'Lấy tổng quan người thân bệnh nhân thành công.'),
  getRelativeDuplicates: wrap((req) => service.getRelativeDuplicates(req.query), 'Lấy nhóm người thân trùng dữ liệu thành công.'),
  getRelative: wrap((req) => service.getRelative(req.params.relativeId), 'Lấy chi tiết người thân bệnh nhân thành công.'),
  updateRelative: wrap((req) => service.updateRelative(req.params.relativeId, req.body || {}, req.auth, requestMeta(req)), 'Cập nhật người thân bệnh nhân thành công.'),
  verifyRelative: wrap((req) => service.verifyRelative(req.params.relativeId, req.body || {}, req.auth, requestMeta(req)), 'Xác minh quan hệ người thân thành công.'),
  unverifyRelative: wrap((req) => service.unverifyRelative(req.params.relativeId, req.body || {}, req.auth, requestMeta(req)), 'Hủy xác minh quan hệ người thân thành công.'),
  blockRelative: wrap((req) => service.blockRelative(req.params.relativeId, req.body || {}, req.auth, requestMeta(req)), 'Block người thân bệnh nhân thành công.'),
  unblockRelative: wrap((req) => service.unblockRelative(req.params.relativeId, req.body || {}, req.auth, requestMeta(req)), 'Unblock người thân bệnh nhân thành công.'),
  getRelativeAccessHistory: wrap((req) => service.getRelativeAccessHistory(req.params.relativeId, req.query), 'Lấy access history người thân thành công.'),

  listAuthorizations: wrap((req) => service.listAuthorizations(req.query), 'Lấy danh sách ủy quyền người thân thành công.'),
  getAuthorizationsSummary: wrap(() => service.getAuthorizationsSummary(), 'Lấy tổng quan ủy quyền người thân thành công.'),
  getAuthorization: wrap((req) => service.getAuthorization(req.params.authorizationId), 'Lấy chi tiết ủy quyền người thân thành công.'),
  approveAuthorization: wrap((req) => service.approveAuthorization(req.params.authorizationId, req.auth, requestMeta(req)), 'Duyệt ủy quyền người thân thành công.'),
  rejectAuthorization: wrap((req) => service.rejectAuthorization(req.params.authorizationId, req.body || {}, req.auth, requestMeta(req)), 'Từ chối ủy quyền người thân thành công.'),
  revokeAuthorization: wrap((req) => service.revokeAuthorization(req.params.authorizationId, req.body || {}, req.auth, requestMeta(req)), 'Thu hồi ủy quyền người thân thành công.'),
  extendAuthorization: wrap((req) => service.extendAuthorization(req.params.authorizationId, req.body || {}, req.auth, requestMeta(req)), 'Gia hạn ủy quyền người thân thành công.'),
  updateAuthorizationScopes: wrap((req) => service.updateAuthorizationScopes(req.params.authorizationId, req.body || {}, req.auth, requestMeta(req)), 'Cập nhật scope ủy quyền thành công.'),
  getAuthorizationEffectiveAccess: wrap((req) => service.getAuthorizationEffectiveAccess(req.params.authorizationId), 'Lấy quyền hiệu lực thành công.'),
  getAuthorizationAccessLogs: wrap((req) => service.getAuthorizationAccessLogs(req.params.authorizationId, req.query), 'Lấy access logs ủy quyền thành công.'),
  bulkRevokeAuthorizations: wrap((req) => service.bulkRevokeAuthorizations(req.body || {}, req.auth, requestMeta(req)), 'Bulk revoke ủy quyền thành công.'),

  listProfileFieldPolicies: wrap((req) => service.listProfileFieldPolicies(req.query), 'Lấy profile field policies thành công.'),
  getProfileFieldPolicy: wrap((req) => service.getProfileFieldPolicy(req.params.fieldName), 'Lấy profile field policy thành công.'),
  updateProfileFieldPolicy: wrap((req) => service.updateProfileFieldPolicy(req.params.fieldName, req.body || {}, req.auth, requestMeta(req)), 'Cập nhật profile field policy thành công.'),
  rebuildProfileFieldPolicies: wrap((req) => service.ensureDefaultProfileFieldPolicies(req.auth, requestMeta(req)), 'Rebuild default profile field policies thành công.'),
  listProfileFieldPoliciesAudit: wrap((req) => service.listProfileFieldPoliciesAudit(req.query), 'Lấy audit profile field policies thành công.'),

  listProfileChangeRequests: wrap((req) => service.listProfileChangeRequests(req.query), 'Lấy queue yêu cầu cập nhật hồ sơ thành công.'),
  getProfileChangeSummary: wrap(() => service.getProfileChangeSummary(), 'Lấy tổng quan yêu cầu cập nhật hồ sơ thành công.'),
  getProfileChangeRequest: wrap((req) => service.getProfileChangeRequest(req.params.requestId), 'Lấy chi tiết yêu cầu cập nhật hồ sơ thành công.'),
  approveProfileChangeRequest: wrap((req) => service.approveProfileChangeRequest(req.params.requestId, req.body || {}, req.auth, requestMeta(req)), 'Duyệt yêu cầu cập nhật hồ sơ thành công.'),
  rejectProfileChangeRequest: wrap((req) => service.rejectProfileChangeRequest(req.params.requestId, req.body || {}, req.auth, requestMeta(req)), 'Từ chối yêu cầu cập nhật hồ sơ thành công.'),
  requestMoreInfoProfileChange: wrap((req) => service.requestMoreInfoForProfileChange(req.params.requestId, req.body || {}, req.auth, requestMeta(req)), 'Yêu cầu bổ sung thông tin thành công.'),
  assignProfileChangeRequest: wrap((req) => service.assignProfileChangeRequest(req.params.requestId, req.body || {}, req.auth, requestMeta(req)), 'Assign yêu cầu cập nhật hồ sơ thành công.'),
  bulkApproveProfileChanges: wrap((req) => service.bulkApproveProfileChanges(req.body || {}, req.auth, requestMeta(req)), 'Bulk approve profile changes thành công.'),
  bulkRejectProfileChanges: wrap((req) => service.bulkRejectProfileChanges(req.body || {}, req.auth, requestMeta(req)), 'Bulk reject profile changes thành công.'),

  listDocuments: wrap((req) => service.listDocuments(req.query), 'Lấy tài liệu bệnh nhân upload thành công.'),
  getDocumentsSummary: wrap(() => service.getDocumentsSummary(), 'Lấy tổng quan tài liệu portal thành công.'),
  getDocument: wrap((req) => service.getDocument(req.params.documentId), 'Lấy chi tiết tài liệu portal thành công.'),
  approveDocument: wrap((req) => service.approveDocument(req.params.documentId, req.body || {}, req.auth, requestMeta(req)), 'Duyệt tài liệu portal thành công.'),
  rejectDocument: wrap((req) => service.rejectDocument(req.params.documentId, req.body || {}, req.auth, requestMeta(req)), 'Từ chối tài liệu portal thành công.'),
  rescanDocument: wrap((req) => service.rescanDocument(req.params.documentId, req.body || {}, req.auth, requestMeta(req)), 'Yêu cầu rescan tài liệu thành công.'),
  updateDocumentMetadata: wrap((req) => service.updateDocumentMetadata(req.params.documentId, req.body || {}, req.auth, requestMeta(req)), 'Cập nhật metadata tài liệu thành công.'),
  getDocumentAccessLogs: wrap((req) => service.getDocumentAccessLogs(req.params.documentId, req.query), 'Lấy access logs tài liệu thành công.'),
  bulkApproveDocuments: wrap((req) => service.bulkApproveDocuments(req.body || {}, req.auth, requestMeta(req)), 'Bulk approve tài liệu thành công.'),
  bulkRejectDocuments: wrap((req) => service.bulkRejectDocuments(req.body || {}, req.auth, requestMeta(req)), 'Bulk reject tài liệu thành công.'),
  bulkRescanDocuments: wrap((req) => service.bulkRescanDocuments(req.body || {}, req.auth, requestMeta(req)), 'Bulk rescan tài liệu thành công.'),
  releaseDocument: wrap((req) => service.releaseDocument(req.params.documentId, req.auth, requestMeta(req)), 'Release tài liệu cho bệnh nhân thành công.'),
  revokeDocumentRelease: wrap((req) => service.revokeDocumentRelease(req.params.documentId, req.body || {}, req.auth, requestMeta(req)), 'Thu hồi release tài liệu thành công.'),

  listDocumentExports: wrap((req) => service.listDocumentExports(req.query), 'Lấy yêu cầu xuất hồ sơ thành công.'),
  getDocumentExportsSummary: wrap(() => service.getDocumentExportsSummary(), 'Lấy tổng quan xuất hồ sơ thành công.'),
  getDocumentExport: wrap((req) => service.getDocumentExport(req.params.exportId), 'Lấy chi tiết yêu cầu xuất hồ sơ thành công.'),
  retryDocumentExport: wrap((req) => service.retryDocumentExport(req.params.exportId, req.body || {}, req.auth, requestMeta(req)), 'Retry export hồ sơ thành công.'),
  expireDocumentExport: wrap((req) => service.expireDocumentExport(req.params.exportId, req.body || {}, req.auth, requestMeta(req)), 'Expire export hồ sơ thành công.'),
  extendDocumentExport: wrap((req) => service.extendDocumentExport(req.params.exportId, req.body || {}, req.auth, requestMeta(req)), 'Gia hạn export hồ sơ thành công.'),
  revokeDocumentExport: wrap((req) => service.revokeDocumentExport(req.params.exportId, req.body || {}, req.auth, requestMeta(req)), 'Revoke export hồ sơ thành công.'),
  getDocumentExportLogs: wrap((req) => service.getDocumentExportLogs(req.params.exportId, req.query), 'Lấy logs export hồ sơ thành công.'),

  listInsuranceSubmissions: wrap((req) => service.listInsuranceSubmissions(req.query), 'Lấy bảo hiểm bệnh nhân gửi thành công.'),
  getInsuranceSubmissionsSummary: wrap(() => service.getInsuranceSubmissionsSummary(), 'Lấy tổng quan bảo hiểm bệnh nhân gửi thành công.'),
  getInsuranceSubmission: wrap((req) => service.getInsuranceSubmission(req.params.policyId), 'Lấy chi tiết bảo hiểm bệnh nhân gửi thành công.'),
  verifyInsuranceSubmission: wrap((req) => service.verifyInsuranceSubmission(req.params.policyId, req.body || {}, req.auth, requestMeta(req)), 'Verify bảo hiểm thành công.'),
  rejectInsuranceSubmission: wrap((req) => service.rejectInsuranceSubmission(req.params.policyId, req.body || {}, req.auth, requestMeta(req)), 'Reject bảo hiểm thành công.'),
  requestMoreInfoInsurance: wrap((req) => service.requestMoreInfoInsurance(req.params.policyId, req.body || {}, req.auth, requestMeta(req)), 'Yêu cầu bổ sung bảo hiểm thành công.'),
  duplicateCheckInsurance: wrap((req) => service.duplicateCheckInsurance(req.params.policyId), 'Kiểm tra trùng bảo hiểm thành công.'),

  listFeatureFlags: wrap((req) => service.listFeatureFlags(req.query), 'Lấy portal feature flags thành công.'),
  getFeatureFlag: wrap((req) => service.getFeatureFlag(req.params.key), 'Lấy portal feature flag thành công.'),
  updateFeatureFlag: wrap((req) => service.updateFeatureFlag(req.params.key, req.body || {}, req.auth, requestMeta(req)), 'Cập nhật portal feature flag thành công.'),
  rollbackFeatureFlag: wrap((req) => service.rollbackFeatureFlag(req.params.key, req.auth, requestMeta(req)), 'Rollback portal feature flag thành công.'),
  rebuildFeatureFlags: wrap((req) => service.ensureDefaultFeatureFlags(req.auth, requestMeta(req)), 'Rebuild default portal feature flags thành công.'),
  importFeatureFlags: wrap((req) => service.importFeatureFlags(req.body || {}, req.auth, requestMeta(req)), 'Import portal feature flags thành công.'),
  exportFeatureFlags: wrap(() => service.exportFeatureFlags(), 'Export portal feature flags thành công.'),
  listFeatureFlagAudit: wrap((req) => service.listFeatureFlagAudit(req.params.key, req.query), 'Lấy audit feature flag thành công.'),

  listAudit: wrap((req) => service.listAudit(req.query), 'Lấy portal audit thành công.'),
  getAuditSummary: wrap((req) => service.getAuditSummary(req.query), 'Lấy tổng quan portal audit thành công.'),
  getAudit: wrap((req) => service.getAudit(req.params.auditLogId), 'Lấy chi tiết portal audit thành công.'),
  exportAudit: wrap((req) => service.exportAudit(req.query, req.auth, requestMeta(req)), 'Export portal audit thành công.'),
  getPatientAccessTimeline: wrap((req) => service.getPatientAccessTimeline(req.params.patientId, req.query), 'Lấy patient access timeline thành công.'),
};
