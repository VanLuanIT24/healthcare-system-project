const { USER_STATUS } = require('../statuses');

const STAFF_TRANSITIONS = {
  [USER_STATUS.ACTIVE]: [
    USER_STATUS.SUSPENDED,
    USER_STATUS.LOCKED,
    USER_STATUS.DISABLED,
    USER_STATUS.ACTIVE,
  ],
  [USER_STATUS.SUSPENDED]: [
    USER_STATUS.ACTIVE,
    USER_STATUS.DISABLED,
    USER_STATUS.LOCKED,
    USER_STATUS.SUSPENDED,
  ],
  [USER_STATUS.LOCKED]: [
    USER_STATUS.ACTIVE,
    USER_STATUS.DISABLED,
    USER_STATUS.SUSPENDED,
    USER_STATUS.LOCKED,
  ],
  [USER_STATUS.DISABLED]: [USER_STATUS.ACTIVE, USER_STATUS.DISABLED],
};

module.exports = {
  STAFF_TRANSITIONS,
};
